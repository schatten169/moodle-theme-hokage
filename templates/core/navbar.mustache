
{{!
    Template untuk breadcrumb di page header pada full_header.
    ---
    Kenapa ada dahboard_navbar & navbar biasa?
    Karena yang dashboard navbar digunakan untuk bagian navbar (atas),
    sedangkan yang ini untuk breadcrumb saja.
    Kalau di boost, navbar atas dia pakai navbar ini juga.
    Karena di theme kita penggunaannya beda, jadinya dipisah supaya
    nggak bikin pusing.
}}
<nav aria-label="{{#str}}breadcrumb, access{{/str}}">
    <ul class="breadcrumb breadcrumb-separatorless fw-semibold fs-6">
        {{#get_items}}
            {{#has_action}}
                <li class="breadcrumb-item fw-bold lh-1{{#is_hidden}} dimmed_text{{/is_hidden}}">
                    <a href="{{{action}}}"
                        {{#is_last}}aria-current="page"{{/is_last}}
                        {{#get_title}}title="{{get_title}}"{{/get_title}}
                        {{#attributes}}{{name}}="{{value}}"{{/attributes}}
                        class="text-gray-500"
                    >
                        {{{get_content}}}
                    </a>
                </li>
                <li class="breadcrumb-item">
                    <i class="ki-duotone ki-right fs-6 text-gray-500 mx-n1"></i>
                </li>
            {{/has_action}}
            {{^has_action}}
                <li class="breadcrumb-item text-gray-600{{#is_hidden}} dimmed_text{{/is_hidden}}">
                    <span {{#attributes}}{{name}}="{{value}}" {{/attributes}}>
                        {{{text}}}
                    </span>
                </li>
            {{/has_action}}
        {{/get_items}}
    </ul>
</nav>
